{{!--acts as a template--}}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Some website</title>
    </head>
    <body>
        {{> header}}
        {{!--using the screamIt helper--}}
        <p>{{screamIt welcomeMessage}}</p>
 {{!--enjecting partial in a template--}}
        {{> footer}}
    </body>
</html>